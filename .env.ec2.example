# Example Environment Variables for EC2 Deployment
# Copy this file to your EC2 instance at: /home/ubuntu/smartfix-samana/.env
# Fill in the actual values and keep this file secure

# Docker Hub Configuration
DOCKER_USERNAME=your-dockerhub-username

# Database Configuration
DB_HOST=db
DB_PORT=3306
DB_NAME=smartfix_db
DB_USERNAME=smartfix_user
DB_PASSWORD=your-secure-database-password

# MySQL Root Password
MYSQL_ROOT_PASSWORD=your-secure-root-password

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-min-256-bits
JWT_EXPIRATION=86400000

# Staging Environment Variables (if using staging)
DB_NAME_STAGING=smartfix_staging_db
DB_USERNAME_STAGING=smartfix_staging_user
DB_PASSWORD_STAGING=your-secure-staging-database-password
MYSQL_ROOT_PASSWORD_STAGING=your-secure-staging-root-password
JWT_SECRET_STAGING=your-staging-jwt-secret-key

# Application Configuration
SPRING_PROFILES_ACTIVE=prod

# Backup Configuration (optional)
BACKUP_ENABLED=true
BACKUP_SCHEDULE=0 2 * * *
BACKUP_RETENTION_DAYS=7
