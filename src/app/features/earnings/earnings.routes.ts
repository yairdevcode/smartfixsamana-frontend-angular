import { Routes } from '@angular/router';
import { EarningsSummaryComponent } from './components/earnings-summary/earnings-summary.component';

export const earningsRoutes: Routes = [
  { path: '', component: EarningsSummaryComponent },
];
